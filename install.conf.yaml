- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.bash_logout: bash_logout
    ~/.profile: profile
    ~/.gitconfig: gitconfig
    ~/.vimrc: vimrc
    ~/.config/lazygit/config.yml: lazygit
    ~/.git-sh-prompt: git-sh-prompt
